<idea-plugin>
  <id>com.terminaltabtailor.TerminalTabTailor</id>
  <name>Terminal Tab Tailor</name>
  <vendor email="romain.dalichamp@gmail.com" url="http://romain.dalichamp.fr">Terminal Tab Tailor
  </vendor>

  <change-notes><![CDATA[
    <b>What's New ?</b>
    <br/><br/>
    <b>Requirement</b>
    <ul>
      <li><b>IntelliJ IDEA 2025.3 or later: </b> the API that lets a plugin open the terminal engine you selected only exists from 2025.3. If you are on an earlier version, 1.5.1 remains available and keeps working.</li>
    </ul>
    <b>Fixed</b>
    <ul>
      <li><b>"Follow the shell's current folder" no longer claims to work for PowerShell and pwsh on the classic engine. </b> It never actually did: <code>Set-Location</code> doesn't call Win32's <code>SetCurrentDirectory()</code>, an open upstream PowerShell issue, so the OS-level query this plugin's classic engine relies on stayed frozen at the shell's launch directory no matter how many times it was asked - not something this plugin can read around. Verified end to end before landing this fix: every layer between this plugin and the OS query was checked for a cache on our side, there was none. Classic + cmd is unaffected and keeps following normally, and PowerShell/pwsh follow correctly on the reworked engine, which reads shell integration instead of the OS. A PowerShell/pwsh tab on the classic engine now simply keeps the name it opened with, like any other shell this plugin cannot follow, instead of silently freezing on the wrong name.</li>
    </ul>
  ]]></change-notes>

  <description><![CDATA[
    Terminal Tab Tailor significantly enhances the IntelliJ IDEA workflow by dynamically renaming terminal tabs to reflect the currently selected item in your project tree. This feature greatly aids in organizing your workspace and navigating effortlessly between numerous terminal sessions.
    <br/><br/>
    Simply select a file or folder within your project tree and initiate a terminal session from that context.
    <br/><br/>
    <b>Pro-tip</b>: Ctrl+Shift+X opens a terminal for the current selection right away. Rebind it under Settings &gt; Keymap if it clashes with your own shortcuts.
    <br/><br/>
    <b>Three ways to open a tailored tab</b>
    <ul>
      <li><b>Open In &gt; Terminal</b> - Terminal Tab Tailor takes over the IDE's own "open in terminal" entries, offering <b>Terminal (Reworked)</b> and <b>Terminal (Classic)</b> so you pick the engine on the spot. Every place you already open a terminal from produces a tailored tab.</li>
      <li><b>The TTT Button</b> - the "Open in Terminal" action in one click, from the terminal toolbar, using your most recent selection.</li>
      <li><b>Rename Session</b> - right-click any tab to name it yourself. Your sort order is re-applied straight away and the renamed tab stays selected.</li>
    </ul>
    Choose what the TTT Button follows:
    <ul>
      <li>Use the last selection from the Project Tree.</li>
      <li>Use the last selected Tab from the File Editor.</li>
      <li>Use the most recent selection, whether from the Project Tree or the File Editor Manager.</li>
      <li>Use the project name, always.</li>
    </ul>
    Options &amp; Sort
    <ul>
      <li>Reuse an existing tab when the names match.</li>
      <li>Prompt the renaming dialogue each time a new terminal tab is opened.</li>
      <li>Add parent folders to a tab's name, so it reads <code>TerminalTabTailor/src</code> rather than <code>src</code>.</li>
      <li>Incorporate the current date into tab names (with a customizable pattern, defaulting to dd-MM-yy).</li>
      <li>Maintain tabs in perpetual alphabetical order.</li>
      <li>Maintain tabs sorted by descending date.</li>
      <li>Do not sort tabs.</li>
    </ul>
    Whichever you choose, opening or reusing a tab brings the terminal window to the front and selects that tab.
    <br/><br/>
    Choose from a variety of naming conventions to suit your workflow:<br/>
    <ul>
      <li>Permit the use of file names directly.</li>
      <li>Default to directory names, utilizing the parent directory's name for files.</li>
      <li>Adopt the name of the parent module.</li>
      <li>Use the parent module's directory name for a more structured approach.</li>
      <li>Consistently use the project's name for all terminal tabs.</li>
      <li>Follow the shell's current folder, renaming the tab as it changes directory (every shell on the reworked engine; cmd only on the classic engine on Windows - PowerShell and pwsh have an upstream limitation there; bash, zsh, sh, fish, dash and ksh on macOS and Linux).</li>
    </ul>
    Configurability is at the heart of Terminal Tab Tailor. Find the settings in <b>Settings &gt; Tools &gt; Terminal Tab Tailor</b>, or via <b>Terminal Tab Tailor Settings</b> in the dropdown next to + in the Terminal tool window.
  ]]></description>

  <depends>com.intellij.modules.platform</depends>
  <depends>org.jetbrains.plugins.terminal</depends>
  <projectListeners>
    <listener
      class="com.terminaltabtailor.listener.ProjectFileEditorManagerListener"
      topic="com.intellij.openapi.fileEditor.FileEditorManagerListener"/>
    <listener
      class="com.terminaltabtailor.listener.CustomProjectViewListener"
      topic="com.intellij.ide.projectView.impl.ProjectViewListener"/>
  </projectListeners>
  <extensions defaultExtensionNs="com.intellij">
    <postStartupActivity implementation="com.terminaltabtailor.activity.StartupActivity"/>
    <projectConfigurable parentId="tools"
      instance="com.terminaltabtailor.settings.TerminalTabTailorConfigurable"
      displayName="Terminal Tab Tailor"/>
    <applicationService
      serviceImplementation="com.terminaltabtailor.settings.TerminalTabTailorSettingsService"/>
    <projectService
      serviceImplementation="com.terminaltabtailor.service.CurrentDirectoryTabNameTracker"/>

  </extensions>
  <extensions defaultExtensionNs="org.jetbrains.plugins.terminal">
    <openPredefinedTerminalProvider
      implementation="com.terminaltabtailor.provider.OpenPredefinedTerminalTabTailorActionProvider"/>
  </extensions>

  <actions>
    <action
      id="com.terminaltabtailor.action.CustomRenameTerminalSessionAction"
      class="com.terminaltabtailor.action.CustomRenameTerminalSessionAction"
      text="Rename Session">
      <add-to-group group-id="ToolWindowContextMenu" anchor="first"/>
    </action>
    <action id="com.terminaltabtailor.action.CustomRevealFileInTerminalAction"
      class="com.terminaltabtailor.action.CustomRevealFileInTerminalAction"
      icon="org.jetbrains.plugins.terminal.TerminalIcons.OpenTerminal_13x13"
      text="Open in terminal">
      <override-text place="RevealInPopup"/>
      <add-to-group group-id="TouchBarDefault_alt" anchor="last"/>
      <!--
        Letter-based on purpose: the back quote is a dead key on several layouts, so a plain
        BACK_QUOTE keystroke never reaches the IDE. Ctrl+Shift+X is free in the default keymap
        (Ctrl+Alt+T, the obvious mnemonic, is taken by UsageGrouping.UsageType).
      -->
      <keyboard-shortcut keymap="$default" first-keystroke="control shift X"/>
    </action>
    <action
      id="com.terminaltabtailor.action.CustomTerminalNewTabAction"
      class="com.terminaltabtailor.action.CustomTerminalNewTabAction"
      description="Open using Terminal Tab Tailor"
      icon="org.jetbrains.plugins.terminal.TerminalIcons.OpenTerminal_13x13"
      text="Open in Terminal">
      <add-to-group group-id="TerminalToolwindowActionGroup" anchor="first"/>
    </action>
  </actions>
</idea-plugin>